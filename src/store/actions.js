// export const setInfo = ({ commit }) => commit('setInfo');

// export const goToFirstpage = ({ commit }) => commit('goToFirstpage');

// export const signOut = ({ commit }) => commit('signOut');

// export const setCourseId = ({ commit }) => commit('setCourseId',id);
